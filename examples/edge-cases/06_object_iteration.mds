---
env:
  NODE_ENV: production
  PORT: "3000"
  DATABASE_URL: "postgres://localhost/app"
  LOG_LEVEL: info
  CACHE_TTL: "300"
---

## Environment Variables

@for key, value in env:
- `{key}` = `{value}`
@end
